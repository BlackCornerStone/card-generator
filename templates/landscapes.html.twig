<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Landscape Cards</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 20px;
            background-color: #f5f5f5;
        }
        
        .grid {
            display: block;
            width: 100%;
        }
        
        .card {
            display: inline-block;
            width: 3.5in;
            height: 2.5in;
            background-color: white;
            border: 2px solid #333;
            border-radius: 8px;
            padding: 8px;
            margin: 8px;
            vertical-align: top;
            page-break-inside: avoid;
            break-inside: avoid;
            font-size: 9px;
            position: relative;
        }
        
        .landscape-name {
            position: absolute;
            top: 8px;
            left: 8px;
            font-size: 12px;
            font-weight: bold;
            color: #2c3e50;
            max-width: 60%;
        }
        
        .realm {
            position: absolute;
            top: 8px;
            right: 8px;
            background: #3498db;
            color: white;
            padding: 2px 5px;
            border-radius: 8px;
            font-size: 8px;
            max-width: 35%;
            text-align: center;
        }
        
        .stats-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 8px;
            height: calc(100% - 40px);
            margin-top: 25px;
        }
        
        .stat-group {
            background: #f8f9fa;
            padding: 6px;
            border-radius: 4px;
            border: 1px solid #dee2e6;
        }
        
        .stat-group h3 {
            margin: 0 0 4px 0;
            color: #495057;
            font-size: 9px;
            font-weight: bold;
            border-bottom: 1px solid #dee2e6;
            padding-bottom: 2px;
        }
        
        .stat-row {
            display: block;
            margin: 2px 0;
            font-size: 8px;
            line-height: 1.2;
        }
        
        .stat-label {
            font-weight: 500;
        }
        
        .stat-value {
            color: #666;
            word-break: break-word;
        }
        
        .terrain-badge {
            position: absolute;
            bottom: 8px;
            right: 8px;
            padding: 2px 6px;
            border-radius: 10px;
            font-size: 8px;
            font-weight: bold;
        }
        
        .terrain-open { 
            background: #e8f5e8; 
            color: #2d5a2d; 
        }
        
        .terrain-difficult { 
            background: #fff3cd; 
            color: #856404; 
        }
        
        .terrain-extreme { 
            background: #f8d7da; 
            color: #721c24; 
        }
        
        .empty-value {
            color: #ccc;
            font-style: italic;
        }
    </style>
</head>
<body>
    <div class="grid">
        {% for landscape in cards %}
        <div class="card">
            <div class="landscape-name">{{ landscape.Landscape }}</div>
            <div class="realm">{{ landscape.Realm }}{% if landscape.Type %} - {{ landscape.Type }}{% endif %}</div>

            <div class="stats-grid">
                {% for groupName, groupData in landscape %}
                    {% if groupData is iterable %}
                    <div class="stat-group">
                        <h3>{{ groupName }}</h3>
                        <div class="stat-row">
                            {% for key, value in groupData %}
                                <span class="stat-label">{{ key }}:</span>
                                <span class="stat-value {{ value == '-' or value == '' ? 'empty-value' : '' }}">{{ value == '-' or value == '' ? '-' : value }}</span>
                                {% if not loop.last %} | {% endif %}
                            {% endfor %}
                        </div>
                    </div>
                    {% endif %}
                {% endfor %}
            </div>
            
            <div class="terrain-badge terrain-{{ landscape.Terrain|lower }}">
                {{ landscape.Terrain }}
            </div>
        </div>
        {% endfor %}
    </div>
</body>
</html>