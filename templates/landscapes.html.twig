<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Landscape Cards</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 20px;
            background-color: #f5f5f5;
        }
        
        .grid {
            display: block;
            width: 100%;
        }
        
        .card {
            display: inline-flex;
            width: 3.5in;
            height: 2.5in;
            background-color: white;
            border: 2px solid #333;
            border-radius: 8px;
            padding: 8px;
            margin: 8px;
            vertical-align: top;
            page-break-inside: avoid;
            break-inside: avoid;
            font-size: 9px;
            position: relative;
        }
        
        .card-left {
            width: 120px;
            display: flex;
            flex-direction: column;
            padding-right: 8px;
        }
        
        .card-right {
            flex: 1;
            display: flex;
            flex-direction: column;
        }
        
        .card-image {
            width: 100%;
            height: 80px;
            border-radius: 4px;
            object-fit: cover;
            margin-bottom: 8px;
        }
        
        .landscape-name {
            font-size: 12px;
            font-weight: bold;
            color: #2c3e50;
            margin-bottom: 4px;
        }
        
        .badge {
            color: white;
            padding: 2px 5px;
            border-radius: 8px;
            font-size: 8px;
            text-align: center;
            display: inline-block;
            margin-bottom: 4px;
            margin-right: 4px;
        }
        
        .realm-badge {
            background: #3498db;
        }
        
        .type-badge {
            background: #9b59b6;
        }
        
        .stats-grid {
            display: grid;
            grid-template-columns: 1fr;
            gap: 6px;
            flex: 1;
        }
        
        .stat-group {
            background: #f8f9fa;
            padding: 8px;
            border-radius: 4px;
            border: 1px solid #dee2e6;
        }
        
        .stat-group h3 {
            margin: 0 0 4px 0;
            color: #495057;
            font-size: 9px;
            font-weight: bold;
            border-bottom: 1px solid #dee2e6;
            padding-bottom: 2px;
        }
        
        .stat-row {
            display: block;
            margin: 2px 0;
            font-size: 8px;
            line-height: 1.2;
        }
        
        .stat-label {
            font-weight: 500;
        }
        
        .stat-value {
            color: #666;
            word-break: break-word;
        }
        
        .stat-comment {
            color: #999;
            font-size: 7px;
            font-style: italic;
            display: block;
            margin-top: 1px;
        }
        
        .terrain-badge {
            padding: 2px 6px;
            border-radius: 10px;
            font-size: 8px;
            font-weight: bold;
            margin-top: 4px;
            display: inline-block;
        }
        
        .terrain-open { 
            background: #e8f5e8; 
            color: #2d5a2d; 
        }
        
        .terrain-difficult { 
            background: #fff3cd; 
            color: #856404; 
        }
        
        .terrain-extreme { 
            background: #f8d7da; 
            color: #721c24; 
        }
        
        .empty-value {
            color: #ccc;
            font-style: italic;
        }
    </style>
</head>
<body>
    <div class="grid">
        {% for landscape in cards %}
        <div class="card">
            <div class="card-left">
                <div class="landscape-name">{{ landscape.Landscape }}</div>
                <div>
                    <div class="badge realm-badge">{{ landscape.Realm }}</div>
                    {% if landscape.Type %}
                        <div class="badge type-badge">{{ landscape.Type }}</div>
                    {% endif %}
                </div>

                {% if landscape.ImageFile %}
                    <img src="{{ landscape.ImageFile }}" alt="{{ landscape.Landscape }}" class="card-image">
                {% endif %}
                
                <div class="terrain-badge terrain-{{ landscape.Terrain|lower }}">
                    {{ landscape.Terrain }} Terrain
                </div>
            </div>
            
            <div class="card-right">
                <div class="stats-grid">
                    {% for groupName, groupData in landscape %}
                        {% if groupData is iterable %}
                            {% set displayedValues = [] %}
                            {% for key, value in groupData %}
                                {% if value != '-' and value != '' %}
                                    {% set displayedValues = displayedValues|merge([{key: key, value: value}]) %}
                                {% endif %}
                            {% endfor %}
                            {% if displayedValues|length > 0 %}
                                <div class="stat-group">
                                    <h3>{{ groupName }}</h3>
                                    <div class="stat-row">
                                        {% for item in displayedValues %}
                                            <span class="stat-label">{{ item.key }}:</span>
                                            <span class="stat-value">{{ item.value }}</span>
                                            {% if not loop.last %} | {% endif %}
                                        {% endfor %}
                                    </div>
                                    
                                    {% if landscape.Realm == 'Creation' %}
                                        {% if groupName == 'Survival of Prepared' %}
                                            <div class="stat-comment">Auto survival for prepared characters with Survival 1+</div>
                                        {% elseif groupName == 'Survival of Unprepared' and landscape.Type == 'Wilderness' %}
                                            <div class="stat-comment">Unprepared characters take damage penalties</div>
                                        {% endif %}
                                    {% elseif landscape.Realm starts with 'Autochthonia' %}
                                        {% if groupName starts with 'Survival' %}
                                            <div class="stat-comment">Requires Survival 2+ or Conduit Tapping specialty; lack causes -2 successes</div>
                                        {% endif %}
                                    {% endif %}
                                </div>
                            {% endif %}
                        {% endif %}
                    {% endfor %}
                </div>
            </div>
        </div>
        {% endfor %}
    </div>
</body>
</html>