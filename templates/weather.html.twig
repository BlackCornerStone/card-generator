<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Weather Cards</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 20px;
            background-color: #f5f5f5;
        }
        
        .grid {
            display: block;
            width: 100%;
        }
        
        .card {
            display: inline-block;
            width: 1.75in;
            height: 2.5in;
            background-color: white;
            border: 2px solid #333;
            border-radius: 8px;
            padding: 8px;
            margin: 8px;
            vertical-align: top;
            page-break-inside: avoid;
            break-inside: avoid;
            font-size: 9px;
            position: relative;
        }
        
        .card-content {
            display: flex;
            flex-direction: column;
            height: 100%;
        }
        
        .card-image {
            width: 100%;
            height: 60px;
            border-radius: 4px;
            object-fit: cover;
            margin-bottom: 8px;
        }
        
        .header-section {
            margin-bottom: 8px;
        }
        
        .time-location {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 8px;
        }
        
        .weather-name {
            font-size: 12px;
            font-weight: bold;
            margin-bottom: 4px;
            padding: 4px;
            border-radius: 4px;
        }
        
        .weather-name.day {
            background-color: white;
            color: #2c3e50;
            border: 1px solid #ddd;
        }
        
        .weather-name.night {
            background-color: black;
            color: white;
        }
        
        .badge {
            color: white;
            padding: 2px 5px;
            border-radius: 8px;
            font-size: 8px;
            text-align: center;
            display: inline-block;
            margin-bottom: 4px;
            margin-right: 4px;
        }
        
        .time-icon {
            font-size: 16px;
            margin-right: 4px;
        }
        
        .location-badge {
            background: #27ae60;
        }
        
        .stats-grid {
            display: grid;
            grid-template-columns: 1fr;
            gap: 6px;
            flex: 1;
        }
        
        .stat-group {
            background: #f8f9fa;
            padding: 8px;
            border-radius: 4px;
            border: 1px solid #dee2e6;
        }
        
        .stat-group h3 {
            margin: 0 0 4px 0;
            color: #495057;
            font-size: 9px;
            font-weight: bold;
            border-bottom: 1px solid #dee2e6;
            padding-bottom: 2px;
        }
        
        .stat-row {
            display: block;
            margin: 2px 0;
            font-size: 8px;
            line-height: 1.2;
        }
        
        .stat-label {
            font-weight: 500;
        }
        
        .stat-value {
            color: #666;
            word-break: break-word;
        }
        
        .stat-comment {
            color: #999;
            font-size: 7px;
            font-style: italic;
            display: block;
            margin-top: 1px;
        }
        
        .note-section {
            background: #fff3cd;
            border: 1px solid #ffeaa7;
            border-radius: 4px;
            padding: 6px;
            margin-top: 4px;
            font-size: 7px;
            line-height: 1.3;
        }
        
        .note-title {
            font-weight: bold;
            color: #856404;
            margin-bottom: 2px;
        }
        
        .empty-value {
            color: #ccc;
            font-style: italic;
        }
    </style>
</head>
<body>
    <div class="grid">
        {% for weather in cards %}
        <div class="card">
            <div class="card-content">
                <div class="header-section">
                    <div class="weather-name {% if weather.Time %}{{ weather.Time|lower }}{% endif %}">
                        {{ weather.Weather }}
                        {% if weather.Time == 'Day' %}☀️{% else %}🌙{% endif %}
                    </div>
                </div>

                {% if weather.ImageFile %}
                    <img src="{{ weather.ImageFile }}" alt="{{ weather.Weather }}" class="card-image">
                {% endif %}
                
                <div class="stats-grid">
                    <div class="stat-group">
                    {% if weather.Hazard.Trauma != '' %}
                        <h3>Environmental damage {{ weather.Hazard.Damage }}</h3>
                        <div class="stat-row">
                            <span class="stat-label">Stamina+Resistance</span>
                            <span class="stat-value">difficulty {{ weather.Hazard.Trauma }}</span>
                        </div>
                        <div class="stat-comment">Each {{ weather.Hazard.Trauma }} successes decreases damage for one type (A &gt; L &gt; B &gt; *)</div>
                    {% endif %}
                    {% if weather['Athletics penalty'] != '' %}
                        <h3>Athletics difficulty +{{ weather['Athletics penalty']|abs }}</h3>
                    {% endif %}
                    </div>
                </div>
                
                {% if weather.Note and weather.Note != '' %}
                    <div class="note-section">
                        {{ weather.Note }}
                    </div>
                {% endif %}

                {% if weather.Location and weather.Location != '' %}
                <div class="note-section">
                    <div class="note-title">Location:</div>
                    {{ weather.Location }}
                </div>
                {% endif %}
            </div>
        </div>
        {% endfor %}
    </div>
</body>
</html>